{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tmki.dev/schemas/runtime/common.schema.json",
  "title": "TMKI Runtime Common Types",
  "description": "Shared definitions for Run, Step, Event contracts (v0.1)",
  "$defs": {
    "uuid": {
      "type": "string",
      "format": "uuid",
      "description": "RFC 4122 UUID"
    },
    "dateTime": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp (UTC recommended)"
    },
    "policyContext": {
      "type": "object",
      "description": "Session policy context (see ORG_MODEL.md)",
      "additionalProperties": false,
      "required": [
        "company_id",
        "project_id",
        "project_role",
        "employee_id",
        "env"
      ],
      "properties": {
        "company_id": { "type": "string", "minLength": 1 },
        "project_id": { "type": "string", "minLength": 1 },
        "department_id": { "type": "string" },
        "project_role": { "type": "string", "minLength": 1 },
        "employee_id": { "type": "string", "minLength": 1 },
        "clearance": {
          "type": "string",
          "enum": ["public", "internal", "restricted", "confidential"],
          "default": "internal"
        },
        "env": {
          "type": "string",
          "enum": ["development", "staging", "production"]
        },
        "contractor_id": {
          "type": "string",
          "description": "Required when project_role is contractor guest (see ORG_MODEL.md)"
        },
        "company_group_id": {
          "type": "string",
          "description": "SchBK group scope; required for group_admin"
        }
      }
    },
    "tokenUsage": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "prompt_tokens": { "type": "integer", "minimum": 0 },
        "completion_tokens": { "type": "integer", "minimum": 0 },
        "total_tokens": { "type": "integer", "minimum": 0 },
        "estimated_cost_usd": { "type": "number", "minimum": 0 }
      }
    },
    "budget": {
      "type": "object",
      "description": "Run-level limits (Loop Engine)",
      "additionalProperties": false,
      "properties": {
        "max_steps": { "type": "integer", "minimum": 1, "default": 10 },
        "max_tokens": { "type": "integer", "minimum": 1, "default": 32000 },
        "max_cost_usd": { "type": "number", "minimum": 0, "default": 2.5 },
        "timeout_ms": { "type": "integer", "minimum": 1, "default": 120000 },
        "max_step_duration_ms": { "type": "integer", "minimum": 1, "default": 60000 },
        "max_retries_per_step": { "type": "integer", "minimum": 0, "default": 2 }
      }
    },
    "circuitBreakerConfig": {
      "type": "object",
      "description": "Circuit breaker thresholds (Loop Engine)",
      "additionalProperties": false,
      "properties": {
        "consecutive_step_failures": { "type": "integer", "minimum": 1, "default": 3 },
        "same_error_code_threshold": { "type": "integer", "minimum": 1, "default": 2 },
        "same_tool_failure_threshold": { "type": "integer", "minimum": 1, "default": 2 },
        "recovery_mode": {
          "type": "string",
          "enum": ["degraded_readonly", "fail_run"],
          "default": "degraded_readonly"
        }
      }
    },
    "citation": {
      "type": "object",
      "description": "Citable fragment (see 09_document_processing.md)",
      "additionalProperties": false,
      "required": ["doc_id"],
      "properties": {
        "doc_id": { "type": "string" },
        "page": { "type": "integer", "minimum": 1 },
        "start_offset": { "type": "integer", "minimum": 0 },
        "end_offset": { "type": "integer", "minimum": 0 },
        "url": { "type": "string", "format": "uri" },
        "snippet": { "type": "string" }
      }
    },
    "runtimeError": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "message"],
      "properties": {
        "code": { "type": "string" },
        "message": { "type": "string" },
        "retryable": { "type": "boolean", "default": false },
        "details": { "type": "object", "additionalProperties": true }
      }
    },
    "actor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["user", "system", "service"]
        },
        "employee_id": { "type": "string" },
        "service_name": { "type": "string" }
      }
    }
  }
}

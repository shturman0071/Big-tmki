{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tmki.dev/schemas/voice/voice-session.schema.json",
  "title": "TMKI — голосовая сессия",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "trace_id", "employee_id", "session_type"],
  "properties": {
    "schema_version": { "type": "string", "const": "0.1" },
    "trace_id": { "type": "string", "format": "uuid" },
    "session_id": {
      "type": "string",
      "format": "uuid",
      "description": "Алиас trace_id для UI/интеграций"
    },
    "employee_id": { "type": "string" },
    "session_type": {
      "type": "string",
      "enum": ["todo_command", "meeting", "query_response", "standup", "todo"]
    },
    "started_at": { "type": "string", "format": "date-time" },
    "ended_at": { "type": "string", "format": "date-time" },
    "status": { "type": "string", "enum": ["active", "completed", "cancelled"] },
    "transcript": { "type": "string", "description": "Полный транскрипт сессии" },
    "speaker_identified": { "type": "boolean" },
    "stt_transcript": { "type": "string" },
    "stt_provider": { "type": "string" },
    "tts_persona": {
      "type": "string",
      "enum": ["generated_default", "piper_preset", "system_default"],
      "default": "generated_default",
      "description": "Сгенерированный голос Piper, не клон актёра"
    },
    "tts_audio_ref": { "type": "string" },
    "display_targets": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["tv", "tablet", "computer", "browser"]
      }
    },
    "todo_items_created": {
      "type": "array",
      "items": { "type": "string" }
    },
    "meeting_action_items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "speaker_employee_id": { "type": "string" },
          "assigned_to": { "type": "string" },
          "text": { "type": "string" },
          "status": { "type": "string", "enum": ["pending", "done", "cancelled"] },
          "created_at": { "type": "string", "format": "date-time" }
        }
      }
    },
    "action_items": {
      "type": "array",
      "description": "Алиас meeting_action_items",
      "items": {
        "type": "object",
        "properties": {
          "text": { "type": "string" },
          "assigned_to": { "type": "string" },
          "status": { "type": "string", "enum": ["pending", "done", "cancelled"] },
          "created_at": { "type": "string", "format": "date-time" }
        }
      }
    },
    "speakers": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "employee_id": { "type": "string" },
          "speaker_label": { "type": "string" },
          "identified": { "type": "boolean" }
        }
      }
    }
  }
}
